-- Ejemplo 02: Give y direcciones de pago
-- Demuestra cómo `give` invierte las partes de un contrato.
-- `one USD`      → la contraparte me paga a mí
-- `give one USD` → yo le pago a la contraparte
-- `give give one USD` → equivalente a `one USD` (doble inversión)

let recibo  = one USD;
let pago    = give one USD;
let neutral = give give one USD;

-- Los tres deberían generar el mismo cashflow que `one USD`
-- excepto por la dirección en `pago`
recibo and pago
